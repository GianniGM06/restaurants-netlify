[build]
  publish = "public"
  functions = "netlify/functions"

# ALLOWED_GITHUB_USERS (logins autorisés en écriture, séparés par des virgules)
# peut être défini dans le dashboard Netlify — défaut : giannigm06

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    X-Frame-Options = "DENY"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "geolocation=(self), camera=(), microphone=()"
    Content-Security-Policy = """
      default-src 'self';
      script-src 'self' 'unsafe-inline' https://cdn.jsdelivr.net https://unpkg.com;
      style-src 'self' 'unsafe-inline' https://cdn.jsdelivr.net https://unpkg.com;
      img-src 'self' data: https: blob:;
      font-src 'self' https://cdn.jsdelivr.net;
      connect-src 'self' https://api.github.com https://nominatim.openstreetmap.org https://*.neon.tech;
      worker-src 'none';
      frame-ancestors 'none';
    """

[dev]
  framework = "#static"